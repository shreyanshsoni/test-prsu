version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Node: $(node -v)"
        - echo "NPM: $(npm -v)"
        # Clean + deterministic install
        - npm ci
    build:
      commands:
        - echo "Building Next.js app..."
        - npm run build
        - echo "✅ Build complete"
  artifacts:
    # Keep .next as output dir (needed for SSR + required-server-files.json)
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
